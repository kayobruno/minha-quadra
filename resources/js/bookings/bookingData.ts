export interface BookingData {
    when?: string | null,
    start?: string | null;
    end?: string | null;
    customerId?: string | null,
    courtId?: string | null,
    sport?: string | null,
    note?: string | null,
}
